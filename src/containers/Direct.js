import React, { Component} from "react";

export default class Direct extends Component {
    constructor(props) {
        super(props);
        this.state = {
        };
    }

    componentDidMount() {
    }

    render() {
        return (
            <div>Direct</div>
        );
    }
}
